services:
  db:
    container_name: ownned_local_container
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: local_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5501:5432"
    volumes:
      - /var/lib/postgresql/ownned_data
    networks:
      - ownned_dev
networks:
  ownned_dev:
