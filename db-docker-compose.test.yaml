services:
  db:
    container_name: ownned_test_container
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: test_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5502:5432"
    tmpfs:
      - /var/lib/postgresql/ownned_test_data
    networks:
      - ownned_test
networks:
  ownned_test:
